<div class="banner">
  <div class="container">
    <h1>Codaisseurify</h1>
  </div>
</div>

<div class="container">
    <% @artists.each do |a| %>
      <p><%= link_to "#{a.name}", artist_path(a) %></p>
      <p>Famous quote: <i><%= a.bio %></i></p>
      <p>Music genre: <strong><%= a.genre.capitalize %></strong></p>
    <% end %>
</div>
